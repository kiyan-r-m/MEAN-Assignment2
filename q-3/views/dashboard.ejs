<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Students ERP</title>
    <style>
        table,
        th,
        td,
        tr {
            border: 1px solid;
            border-collapse: collapse;
            padding: 4px 8px;
        }

        .btn {
            margin: 4px;
            padding: 2px 6px;
        }

        .link {
            font-size: 17px;
            font-family: Arial, Helvetica, sans-serif;
        }
    </style>
</head>

<body>
    <h2>Welcome <%=student.email %>
    </h2>
    <div class="link"><a href="/insert">Insert student</a></div>
    <table>
        <tr>
            <th>Rollno</th>
            <th>Name</th>
            <th>Email</th>
            <th colspan="2"></th>
        </tr>
        <%for(let i=0; i < student.data.length; i++) { %>
            <tr>
                <td>
                    <%=student.data[i].rollno%>
                </td>
                <td>
                    <%=student.data[i].name%>
                </td>
                <td>
                    <%=student.data[i].email%>
                </td>
                <td>
                    <a href="/update/<%=student.data[i].rollno%>">Edit</a>
                </td>
                <td>
                    <a href="/delete/<%=student.data[i].rollno%>">Delete</a>
                </td>
            </tr>
            <%}%>
    </table>
    <form action="/logout" method="post">
        <button class="btn">Logout</button>
    </form>
</body>

</html>